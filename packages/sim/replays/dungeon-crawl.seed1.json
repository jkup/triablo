{
  "scenario": "dungeon-crawl",
  "seed": 1,
  "ticks": 3600,
  "hash": "a3171faa7f656eed",
  "note": "Guards the phase-2 exit criterion: a bot clears the five-room charnel-vaults dungeon headlessly — 8/8 kills (last at tick 1466), avatarDamageDealt exactly 362, avatarLife 59/200, avatar ending on the exit tile (20, 15) at tick 1739. First recorded by task 0450, whose melee-boundary fix (decision 0032) made the crawl completable; re-blessed by task 0380, whose grid pathing (decision 0035) makes monsters walk around walls instead of clipping them. Re-blessed again by task 0680 from f7dc3d682f986a80 to a3171faa7f656eed: the avatar now carries a `Progression` component and gains XP from `xpAwardSystem` (registered between `attack` and `death`), so the snapshot serializes one new component whose `xp` climbs over the run — 119 XP across the eight kills, ending level 5 at 119/500. No combat behaviour changed: every metric above, and all eight death ticks (244, 484, 649, 784, 920, 1290, 1362, 1466), reproduced exactly across that re-bless, because the award is pure integer arithmetic, draws no rng, and never writes `Combatant` (decisions 0051/0057 — the character level is not the attacker level). A mismatch means melee engagement (approach clamp, boundary tolerance, attack cadence), movement/pathing (moveOrderSystem, approachSystem's grid chase, the grid), or an involved monster's or the slice avatar's (decision 0030) authored numbers changed — or, now, that the XP award or the progression state itself moved (a per-kill value is balance, so retuning it lands here and is expected to be re-blessed with the new total stated)."
}
